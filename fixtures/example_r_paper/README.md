# example_r_paper

A minimal, self-contained R analysis fixture (base R only) used by repro-check's CI to smoke-test the R runnability route. Runs a linear regression on simulated data and prints the slope.
