# example_notebook

A minimal Jupyter notebook fixture whose cells were last run OUT OF ORDER (execution_count 2 then 1). It still runs correctly top-to-bottom, so repro-check reports `RAN` but attaches an honest caveat that the saved outputs may not match a linear run. Used by CI to smoke-test the notebook route.
